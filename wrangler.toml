name = "timah-dev-new"
compatibility_date = "2026-06-13"

# Sito HTML puro: nessuno step di build, nessuno script Worker.
# I file statici vengono serviti direttamente dalla root del progetto.
[assets]
directory = "."
